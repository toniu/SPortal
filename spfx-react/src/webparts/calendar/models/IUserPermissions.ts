/**
 * The model for the user permissions
 */
export interface IUserPermissions {
  hasPermissionAdd: boolean;
  hasPermissionEdit: boolean;
  hasPermissionDelete: boolean;
  hasPermissionView: boolean;
}